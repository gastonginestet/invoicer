<div class="w-full min-h-screen flex flex-col justify-center items-center bg-gray-100 px-4">
  <div class="w-full md:w-1/2 bg-white py-10 px-8 rounded-xl shadow-lg">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "space-y-4" }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %> 

      <h1 class="text-gray-800 font-bold text-2xl mb-6 text-center">Log In</h1>

      <!-- Email -->
      <div class="flex items-center border-2 py-2 px-3 rounded-2xl">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-gray-400" fill="none"
             viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M16 12a4 4 0 10-8 0 4 4 0 008 0zm0 0v1.5a2.5 2.5 0 005 0V12a9 9 0 10-9 9m4.5-1.206a8.959 8.959 0 01-4.5 1.207" />
        </svg>
        <%= f.email_field :email,
            class: "pl-3 w-full outline-none bg-transparent text-gray-700",
            placeholder: "Email",
            autocomplete: "email",
            autofocus: true %>
      </div>

      <!-- Password -->
      <div class="flex items-center border-2 py-2 px-3 rounded-2xl">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-gray-400" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd"
                d="M5 9V7a5 5 0 0110 0v2a2 2 0 012 2v5a2 2 0 01-2 2H5a2 2 0 01-2-2v-5a2 2 0 012-2zm8-2v2H7V7a3 3 0 016 0z"
                clip-rule="evenodd" />
        </svg>
        <%= f.password_field :password,
            class: "pl-3 w-full outline-none bg-transparent text-gray-700",
            placeholder: "Password",
            autocomplete: "current-password" %>
      </div>

      <!-- Remember me -->
      <% if devise_mapping.rememberable? %>
        <div class="flex items-center mt-2 ml-1">
          <%= f.check_box :remember_me, class: "rounded text-indigo-600" %>
          <%= f.label :remember_me, class: "ml-2 text-sm text-gray-600" %>
        </div>
      <% end %>

      <!-- Submit -->
      <div>
        <%= f.submit "Log In", class: "w-full bg-indigo-600 hover:bg-indigo-700 text-white py-2 rounded-2xl font-semibold cursor-pointer" %>
      </div>

      <!-- Links -->
      <div class="text-center text-sm text-gray-600 mt-4">
        <%= render "devise/shared/links" %>
      </div>
    <% end %>
  </div>
</div>
