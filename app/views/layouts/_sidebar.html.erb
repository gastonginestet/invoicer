<% if user_signed_in? %>
  <aside class="w-64 min-h-screen bg-white border-r shadow-sm flex flex-col">
    <div class="px-6 py-6">
      <h1 class="text-2xl font-semibold text-indigo-600 tracking-tight">Invoicer</h1>
    </div>

    <nav class="flex-1 px-6 space-y-2 text-sm text-gray-700">
      <%= link_to "Dashboard", root_path,
          class: "block px-3 py-2 rounded-md font-medium #{current_page?(root_path) ? 'bg-gray-100 text-indigo-600' : 'hover:bg-gray-100 hover:text-indigo-600'}" %>

      <%= link_to "Invoices", invoices_path,
          class: "block px-3 py-2 rounded-md font-medium #{current_page?(invoices_path) ? 'bg-gray-100 text-indigo-600' : 'hover:bg-gray-100 hover:text-indigo-600'}" %>
    </nav>
  </aside>
<% end %>
